<UserControl x:Class="RevitGpt.CodeEditorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="5" FontWeight="Bold">Enter C# Code:</TextBlock>

        <TextBox x:Name="codeTextBox" Grid.Row="1" Margin="5" 
                 AcceptsReturn="True" AcceptsTab="True" 
                 VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" TextWrapping="NoWrap"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
            <Button x:Name="executeButton" Content="Execute" Padding="10,3" Margin="0,0,5,0"
                    Click="ExecuteButton_Click"/>
            <Button x:Name="clearButton" Content="Clear" Padding="10,3"
                    Click="ClearButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Margin="5,0,5,0" HorizontalAlignment="Right" FontWeight="Bold">Result:</TextBlock>

        <TextBox x:Name="resultTextBox" Grid.Row="3" Margin="5" 
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" 
                 FontFamily="Consolas"/>
    </Grid>
</UserControl>